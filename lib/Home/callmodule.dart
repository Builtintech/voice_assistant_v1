import 'package:flutter/material.dart';

class CallLauncher extends StatelessWidget {
  const CallLauncher({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
